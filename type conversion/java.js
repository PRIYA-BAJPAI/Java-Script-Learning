let age =window.prompt("How old are you?");
age=Number(age);  //this is converting string to number without this output would be 251 if input is 25 
age+=1;
console.log(age);

//for conversion of x to
// number-use function "Number(x)"
// string-use function "String(x)"
// boolean-use function "Boolean(x)"